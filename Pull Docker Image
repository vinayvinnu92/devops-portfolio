Nautilus project developers are planning to start testing on a new project. As per their meeting with the DevOps team, they want to test containerized environment application features. As per details shared with DevOps team, we need to accomplish the following task: a. Pull busybox:musl image on App Server 1 in Stratos DC and re-tag (create new tag) this image as busybox:local.Docker Host (App Server 1)
        |
        | docker pull busybox:musl
        v
   busybox:musl image
        |
        | docker tag busybox:musl busybox:local
        v
   busybox:local image
        |
        v
   docker images  # Verify both tags exist

commands
# 1. Pull the BusyBox musl image
docker pull busybox:musl

# 2. Re-tag the image as busybox:local
docker tag busybox:musl busybox:local

# 3. Verify the tags
docker images
